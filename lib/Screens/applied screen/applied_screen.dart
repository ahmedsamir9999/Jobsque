import 'package:flutter/material.dart';

class AppliedScreen extends StatelessWidget {
  const AppliedScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
            'Applieeeeeeeeeeeeeed'
        ),
      ),
    );
  }
}
